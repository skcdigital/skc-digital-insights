# .telemetry/

This folder contains the product telemetry system for SKC Digital.

## Contents

| File | Purpose |
|------|---------|
| `product.md` | Product model — what the product is, who uses it, how value flows |
| `audits/` | Tracking audit outputs — what's currently instrumented |

## Lifecycle

```
model → audit → design → guide → implement
```

Start with `product.md` for context before any tracking work.
